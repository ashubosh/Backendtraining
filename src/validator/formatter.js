const string = "         Function Up        "
const string1 = string.trim()

const upperCase = "FUNCTION UP"
const changeLowerCase = upperCase.toLowerCase()

const lowerCase = "function up"
const changeUpperCase = lowerCase.toUpperCase()


module.exports.trim = string1
module.exports.lowerCase = changeLowerCase
module.exports.UpperCase = changeUpperCase